# 安全披露桌面演练记录（内部）

| 项 | 值 |
|----|-----|
| 日期 | 2026-07-22 |
| 类型 | 桌面演练（虚构场景，无生产利用） |
| 报告人角色 | 开发（本机） |
| 接收人角色 | 维护者（本机分角色） |
| 渠道 | 按 SECURITY.md：Private reporting / `[SECURITY]` 邮件模板（本机草稿，未外发真实 PoC） |

## 虚构场景（摘要）

- 仓：`f2b-sandbox`
- 描述：若错误响应 JSON 误带完整 API Key 明文（**当前代码路径未证实**；仅用于走流程）
- 影响假设：密钥泄露 → 未授权沙箱操作

## 流程核对

| 步骤 | 结果 |
|------|------|
| SECURITY 模板字段齐全 | 通过（仓、commit 占位、复现、影响） |
| 3 日确认收到话术草稿 | 通过（模板：「已收到，初判进行中」） |
| 对照控制面≠数据面 / hash / 日志 | 通过（检查项见 SECURITY 设计要点） |
| 是否开公开 Issue 贴细节 | **否**（符合政策） |

## 结论

- 文档与渠道说明**可走通**。
- **后续（同日）**：组织 `f2b-dev` 产品仓已启用 GitHub Private vulnerability reporting（API `enabled: true`）；邮件 `security@f2b.dev` 仍可作为备选，生产 MX 可达性可后置确认。
- **无代码缺陷在本演练中确认**；不据此宣称完成漏洞修复。

归档位置：本文件（可提交公开仓，因无真实漏洞细节）。
